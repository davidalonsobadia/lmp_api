# DataSource settings: set here your own configurations for the database 
# LOCALHOST
spring.datasource.url = jdbc:mysql://localhost:3306/LMP_v0?createDatabaseIfNotExist=true
spring.datasource.username = root
spring.datasource.password = admin

# DATABASE TEST IN IS SERVER
#spring.datasource.url = jdbc:mysql://172.20.10.61:3306/LMP_v0
#spring.datasource.username = regadmin
#spring.datasource.password = regadmin

## database connection from IS server...
# JUAN DB
#spring.datasource.url = jdbc:mysql://172.20.10.61:3306/LMP_v1
#spring.datasource.username = regadmin
#spring.datasource.password = regadmin

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = update

# Define whether or not data.sql will be executed at the inizialitation
spring.datasource.initialize= false

# Naming strategy
# spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy
# spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.DefaultNamingStrategy

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager)

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect


# Logging configuration
#logging.level.org.hibernate.SQL=TRACE 
#logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
#logging.file=lmpApi.log

# Spring Boot Security configuration
security.basic.enabled=false